import React from 'react'
import PropTypes from 'prop-types'

const Spinner = props => {
    return (
        <div>
            loading...
        </div>
    )
}

Spinner.propTypes = {

}

export default Spinner
